# Copy to frontend/.env.
# Local dev: point at the backend dev server directly.
# Production build: leave empty — nginx reverse-proxies /api to the backend
# (see deploy/nginx.conf), so a relative /api path is correct.
VITE_API_BASE_URL=http://localhost:4000
